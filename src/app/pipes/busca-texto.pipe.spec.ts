import { BuscaTextoPipe } from './busca-texto.pipe';

describe('BuscaTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscaTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
